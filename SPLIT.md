# Repo layout

This project lives in a parent folder with two separate git repos:

```
KHM-GROUP/
  KHM-Group-backend/   ← this repo → Hostinger (api.ktexstore.com)
  KHM-Group-frontend/  ← separate repo → Vercel (ktexstore.com)
```

The parent `KHM-GROUP/` folder is **not** a git repo.
